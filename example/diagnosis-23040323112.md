# GitHub Action 故障诊断报告

**Run:** [E2E-Light #23040323112](https://github.com/vllm-project/vllm-ascend/actions/runs/23040323112)
**PR:** vllm-project/vllm-ascend#7024 · 分支 `refactor_moe` — `[refactor] replace sca...`
**Workflow:** E2E-Light
**诊断时间:** 2026-03-13

---

## 故障分布概览

| Job | vLLM 版本 | 耗时 | 失败步骤 | 定性 |
|-----|----------|------|---------|------|
| lint / pre-commit | — | 2m27s | — | ✅ 通过 |
| unit test (0) | 4034c3d | 3m17s | Run unit test | ❓ 待确认（代码 Bug 可能性高） |
| unit test (1) | v0.16.0 | 2m52s | — | ⚠️ 被 strategy 取消，非独立失败 |
| e2e-light | — | 0s | — | ⚠️ 未运行（因 unit test 失败取消） |

---

## 根因分析

**故障定性：疑似代码问题（单元测试失败）**
**置信度：中**（需要查看详细日志确认）
**责任方：PR 作者（待确认）**

### 报错原文

```
Executing the custom container implementation failed.
Please contact your self hosted runner administrator.

Error: failed to run script step: Error: command terminated with non-zero exit code:
command terminated with exit code 1
```

### 与上一次失败（Run #23038671925）的关键差异

| | Run #23038671925（E2E-Full） | Run #23040323112（E2E-Light） |
|--|--|--|
| 失败 exit code | **255** | **1** |
| lint 结果 | 未运行 | **✅ 通过** |
| 跨版本独立失败 | ✅ 两版本均独立失败 | ❌ v0.16.0 被 strategy 取消 |
| 定性 | 基础设施（容器崩溃） | 疑似代码问题（测试失败） |

**exit code 1 的含义：** pytest / unittest 测试失败的标准退出码。"Executing the custom container implementation failed" 是 K8s runner 对任何容器内步骤失败的通用包装提示，不代表基础设施本身出问题——exit code 1 说明测试脚本跑完了并报告了失败，而不是被强制终止。

**判定依据：**
1. lint 通过 — 代码格式/类型检查没有问题
2. 所有安装步骤成功 — 环境正常
3. 失败发生在"Run unit test"— 测试逻辑层面
4. exit code 1（不是 255）— 测试进程正常退出并返回失败状态
5. 此 PR 是 `refactor_moe` 分支的重构，改动了 MOE 相关代码，可能影响了单元测试

### ⚠️ 当前无法确认的内容

目前只有 exit code，**还不知道是哪个 test case 失败、失败原因是什么**。需要查看详细日志才能区分：
- 是测试逻辑因重构而失败（代码 Bug）
- 还是测试环境的某个偶发问题（flaky / 基础设施）

---

## 下一步

**立即获取详细日志：**
```powershell
gh run view --job 66917252606 --log-failed --repo vllm-project/vllm-ascend
```

在日志中找：
- 哪个 test file / test function 失败了（`FAILED tests/ut/...`）
- 具体错误信息（`AssertionError`、`ImportError`、`AttributeError` 等）
- 是否有 traceback 指向 PR 修改的文件

**如果日志显示具体测试用例失败** → 确认为代码问题，PR 作者需修复
**如果日志显示环境/网络错误** → 降级为基础设施问题，可重跑

---

## 待确认项

- [ ] 查看 Job 66917252606 的详细日志，确认具体失败的测试用例
- [ ] 确认失败的测试是否与 refactor_moe 的改动文件直接相关
- [ ] 若为代码问题：是 test 本身写错了，还是被测逻辑有 bug？
